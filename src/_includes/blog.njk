---
title: Mitchypoo's blog
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title }}</title>
    <style>
      body {
        background-color: #2c2c2c; /* dark gray */
        color: #F7DC6F; /* Bright Yellow */
        font-family: Arial, sans-serif;
        line-height: 1.6;
        margin: 0;
        display: flex;
        justify-content: center;
        align-items: flex-start; /* Align items to the top */
        padding: 2rem;
        gap: 20px
      }
      
      .prose {
        max-width: 800px;
        /* {# margin: 0 auto; #} */
        padding: 1rem;
        background-color: #3A3A3A; /* Dark Gray */
        border-radius: 0.5rem;
      }

      .sidenav{
        width: 130px;
        flex-shrink: 0;
        top: 0;
        position: sticky;
        overflow-x: hidden;
        padding: 8px 0;      
      }

      .sidenav a {
        display: block;
        padding: 8px 0;
      }

      .sidenav img {
        width: 100%;
        margin-bottom: 10px;
        border-radius: 5px;
      }
      
      /* Basic typography */
      h1, h2, h3, h4, h5, h6 {
        color: #FFC67D; /* Warm Orange */
        margin-bottom: 0.5rem;
      }

      a {
        color: #8BC34A; /* Light Green */
        text-decoration: none;
        border-bottom: 1px solid #8BC34A;
      }

      a:hover {
        color: #C6E2B5; /* Pale Green */
        border-bottom: 1px solid #C6E2B5;
      }

      /* Code blocks and inline code */
      pre, code {
        background-color: #5C0011; /* Dark Burgundy */
        color: #F7DC6F; /* Bright Yellow */
        padding: 0.2rem 0.4rem;
        border-radius: 0.2rem;
      }

      pre {
        padding: 1rem;
        overflow-x: auto;
      }
    </style>
  </head>
  <body>
    <!-- TODO: Nav -->
    <div class="sidenav">
      <a href="/blog">Blog Home</a>
      <a href="/blog/archive">Archive</a>
      <a href="/blog/about">About</a>
      <img src="/assets/images/bearFaceRub.gif" alt="Bears smushing faces">
    </div>
    <div class="prose">
      {{ content | safe }}
    </div>
  </body>
</html>
